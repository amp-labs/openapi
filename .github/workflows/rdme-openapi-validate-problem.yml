# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2024-06-25T13:32:40.657Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  pull_request:
      paths:
      - 'problem/problem.yaml'

jobs:
  rdme-openapi-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v3

      - name: Run `openapi:validate` command 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi:validate problem.yaml --workingDirectory=problem
