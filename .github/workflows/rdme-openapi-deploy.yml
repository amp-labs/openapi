# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2023-11-01T17:50:25.455Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main
    paths:
      - 'api/**'

jobs:
  rdme-openapi-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v4

      - name: Update online API reference based on api.yaml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi api.yaml --workingDirectory=api --key=${{ secrets.README_API_KEY }} --id=${{ secrets.README_API_ID }}

      - name: Update online API reference based on write.yaml 🚀
        uses: readmeio/rdme@v8
        with:
          rdme: openapi write.yaml --workingDirectory=api --key=${{ secrets.README_API_KEY }} --id=${{ secrets.README_WRITE_ID }}
